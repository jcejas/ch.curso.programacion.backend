console.log("Functions");

function run(text) { //Return void
    console.log(text);
}


const test = (text) => {
    console.log(text);
}

function increment (n) {
    return n + 1;
}

const incrementArrow = n => n + 1;

function add (n1, n2) {
    return n1 + n2;
}

const addArrow = (n1, n2) => n1 + n2;

